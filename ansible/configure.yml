---
- hosts: all
  gather_facts: true
  become: true
  environment:
    http_proxy: "{{ web_proxy | default(omit) }}"
    https_proxy: "{{ web_proxy | default(omit) }}"
  roles:
    - raspberry_pi_speedtest
